import { describe, it, expect } from 'vitest';

// NOTE: This suite is intentionally skipped until TokenSelect is implemented by Agent 3.
describe.skip('TokenSelect', () => {
  it('renders available tokens and calls onChange when selected', () => {
    expect(true).toBe(true);
  });
});
