import { describe, it, expect } from 'vitest';

// NOTE: This suite is intentionally skipped until QuoteSummary is implemented by Agents 3/4.
describe.skip('QuoteSummary', () => {
  it('displays amounts and USD values from quote/aggregator', () => {
    expect(true).toBe(true);
  });
});
