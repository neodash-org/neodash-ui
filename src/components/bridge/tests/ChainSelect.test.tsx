import { describe, it, expect } from 'vitest';

// NOTE: This suite is intentionally skipped until ChainSelect is implemented by Agent 3.
describe.skip('ChainSelect', () => {
  it('renders available chains and calls onChange when selected', () => {
    expect(true).toBe(true);
  });
});
